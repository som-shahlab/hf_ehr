# @package _global_

defaults:
  - ../architecture/mamba

data:
  dataloader:
    max_length: 1024

model:
  config_kwargs:
    d_model: 768
    n_layer: 24
    num_hidden_layers: 24
    state_size: 16